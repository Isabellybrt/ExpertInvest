{
  "buildCommand": "npm run prisma:generate --workspace=backend && npm run build:api && npm run build:frontend",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
